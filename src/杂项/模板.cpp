#include <bits/stdc++.h>
#ifdef LOCAL
#define debug(x) cerr << #x << " = " << x << endl;
#else
#undef assert
#define debug(...)
#define assert(...)
#endif
using namespace std;
using ll = long long;
#define endl "\n"

void solve() {
    // TODO
}
int main() {
#ifndef LOCAL
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
#endif
#ifdef LOCAL
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
#endif
    int _ = 1;
    cin >> _;
    while (_--) {
        solve();
    }
#ifdef LOCAL
    fclose(stdin);
    fclose(stdout);
#endif
    return 0;
}